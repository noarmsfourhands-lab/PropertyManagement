@{
    ViewData["Title"] = "Access denied";
}

<div class="row justify-content-center">
    <div class="col-md-7">
        <div class="card shadow-sm">
            <div class="card-body p-4 text-center">
                <h1 class="h4">Access denied</h1>
                <p class="text-muted mb-3">Your account does not have permission to open that page.</p>
                <a class="btn btn-outline-secondary" asp-controller="Home" asp-action="Index">Back to the start</a>
            </div>
        </div>
    </div>
</div>
